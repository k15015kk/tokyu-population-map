export type MapViewState = {
    latitude: number,
    longitude: number,
    zoom: number,
    pitch: number,
    bearing: number
}

export type PopulationData = {
    mesh1kmid: string,
    prefcode: string,
    citycode: string,
    year: string,
    month: string,
    dayflag: string,
    timezone: string,
    population: string
}